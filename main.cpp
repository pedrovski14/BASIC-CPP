#include <iostream> //isostream is a header file that contains functions for basic input put and output operations.

int main(){
    std::cout << "I like pizza!" <<std::endl; //what we will do in this lesson is to write some basic output. colons (2) and c out means character output togheter this means standart character output then brackets and quotation mark and the sentence. Then run it.
    std::cout << "It's really good!" <<std::endl;//endl indicate the of a line.
    return 0; //we will want to return zero, if we reach return zero it means there is no problems in this program, however if one is return that means there was a problem.
}
